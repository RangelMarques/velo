# CI/CD: testes unitários → preview Vercel → E2E → build/deploy PRD

name: Continuous Deployment

on:
  push:
    branches:
      - main

env:
  NODE_VERSION: '24'
  VERCEL_SCOPE: rangeelmarques-9993s-projects

jobs:
  unit-tests:
    name: Unit Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6.0.2

      - name: Setup Node.js
        uses: actions/setup-node@v6.4.0
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Enable Corepack
        run: corepack enable

      - name: Install dependencies
        run: yarn install

      - name: Run unit tests
        run: yarn test

  build-and-deploy:
    name: Build & Deploy Vercel Preview
    needs: [unit-tests]
    runs-on: ubuntu-latest
    outputs:
      deployment-url: ${{ steps.deploy.outputs.deployment-url }}

    env:
      VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
      VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6.0.2

      - name: Validate Vercel secrets
        run: |
          missing=""
          [ -z "$VERCEL_TOKEN" ] && missing="$missing VERCEL_TOKEN"
          [ -z "$VERCEL_ORG_ID" ] && missing="$missing VERCEL_ORG_ID"
          [ -z "$VERCEL_PROJECT_ID" ] && missing="$missing VERCEL_PROJECT_ID"
          if [ -n "$missing" ]; then
            echo "::error::Secrets ausentes no GitHub Actions:$missing"
            exit 1
          fi

          # Remove espaços/quebras colados ao copiar o secret no GitHub
          VERCEL_TOKEN=$(printf '%s' "$VERCEL_TOKEN" | tr -d '[:space:]')
          VERCEL_ORG_ID=$(printf '%s' "$VERCEL_ORG_ID" | tr -d '[:space:]')
          VERCEL_PROJECT_ID=$(printf '%s' "$VERCEL_PROJECT_ID" | tr -d '[:space:]')

          case "$VERCEL_TOKEN" in
            *[.\ /]*)
              echo "::error::VERCEL_TOKEN inválido: não pode conter espaço, '.' ou '/'."
              echo "Cole apenas o token de https://vercel.com/account/tokens (sem aspas nem URL)."
              exit 1
              ;;
          esac

          {
            echo "VERCEL_TOKEN<<EOF"
            printf '%s\n' "$VERCEL_TOKEN"
            echo "EOF"
            echo "VERCEL_ORG_ID<<EOF"
            printf '%s\n' "$VERCEL_ORG_ID"
            echo "EOF"
            echo "VERCEL_PROJECT_ID<<EOF"
            printf '%s\n' "$VERCEL_PROJECT_ID"
            echo "EOF"
          } >> "$GITHUB_ENV"

      - name: Setup Node.js
        uses: actions/setup-node@v6.4.0
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Enable Corepack
        run: corepack enable

      - name: Install dependencies
        run: yarn install

      - name: Pull Vercel Config
        run: |
          yarn vercel pull --yes \
            --environment=preview \
            --token="$VERCEL_TOKEN" \
            --scope="${{ env.VERCEL_SCOPE }}"

      - name: Build Vercel
        run: yarn vercel build --token="$VERCEL_TOKEN"

      - name: Deploy preview
        id: deploy
        run: |
          URL=$(yarn --silent vercel deploy --prebuilt \
            --target=preview \
            --token="$VERCEL_TOKEN" \
            --scope="${{ env.VERCEL_SCOPE }}")
          echo "deployment-url=$URL" >> "$GITHUB_OUTPUT"

  e2e-tests:
    name: Run E2E Tests
    needs: [build-and-deploy]
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6.0.2

      - name: Setup Node.js
        uses: actions/setup-node@v6.4.0
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Enable Corepack
        run: corepack enable

      - name: Install dependencies
        run: yarn install

      - name: Install Playwright Browser
        run: yarn playwright install --with-deps chromium

      - name: Validate DATABASE_URL
        env:
          DATABASE_URL_PREVIEW: ${{ secrets.DATABASE_URL_PREVIEW }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          if [ -n "$DATABASE_URL_PREVIEW" ]; then
            DB_URL=$(printf '%s' "$DATABASE_URL_PREVIEW" | tr -d '\r\n')
            echo "Usando DATABASE_URL_PREVIEW (Supabase velo-sprint-preview)"
          elif [ -n "$DATABASE_URL" ]; then
            DB_URL=$(printf '%s' "$DATABASE_URL" | tr -d '\r\n')
            echo "Usando DATABASE_URL (fallback)"
          else
            echo "::error::Configure DATABASE_URL_PREVIEW no GitHub Actions (banco do preview)"
            exit 1
          fi
          echo "DATABASE_URL<<EOF" >> "$GITHUB_ENV"
          printf '%s\n' "$DB_URL" >> "$GITHUB_ENV"
          echo "EOF" >> "$GITHUB_ENV"

      - name: Test database connection
        run: node playwright/support/database/verifyConnection.mjs

      - name: Run E2E Regression Tests
        run: yarn playwright test
        env:
          BASE_URL: ${{ needs.build-and-deploy.outputs.deployment-url }}
          CI: true
          NODE_OPTIONS: '--dns-result-order=ipv4first'

      - name: Upload Playwright report
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@v7.0.1
        with:
          name: playwright-report
          path: playwright-report

      - name: Publish Testdino Report
        if: ${{ !cancelled() }}
        env:
          TD_TOKEN: ${{ secrets.TD_TOKEN }}
        run: |
          if [ -n "$TD_TOKEN" ]; then
            npx tdpw upload ./playwright-report --token="$TD_TOKEN"
          else
            echo "TD_TOKEN não configurado; pulando upload TestDino."
          fi

  build-and-deploy-production:
    name: Build & Deploy Vercel Production
    needs: [e2e-tests]
    runs-on: ubuntu-latest

    env:
      VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6.0.2

      - name: Setup Node.js
        uses: actions/setup-node@v6.4.0
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Enable Corepack
        run: corepack enable

      - name: Prepare Vercel token
        run: |
          if [ -z "$VERCEL_TOKEN" ]; then
            echo "::error::VERCEL_TOKEN não configurado no GitHub Actions"
            exit 1
          fi
          VERCEL_TOKEN=$(printf '%s' "$VERCEL_TOKEN" | tr -d '[:space:]')
          echo "VERCEL_TOKEN<<EOF" >> "$GITHUB_ENV"
          printf '%s\n' "$VERCEL_TOKEN" >> "$GITHUB_ENV"
          echo "EOF" >> "$GITHUB_ENV"

      - name: Install dependencies
        run: yarn install

      - name: Pull Vercel Config (production)
        run: |
          yarn vercel pull --yes \
            --environment=production \
            --token="$VERCEL_TOKEN" \
            --scope="${{ env.VERCEL_SCOPE }}"

      - name: Build Vercel (production)
        run: yarn vercel build --prod --token="$VERCEL_TOKEN"

      - name: Deploy production
        run: |
          yarn --silent vercel deploy --prebuilt --prod \
            --token="$VERCEL_TOKEN" \
            --scope="${{ env.VERCEL_SCOPE }}" \
            --yes
